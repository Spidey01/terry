# vim: set ft=sh :
#
# set my various options

HISTFILE="${HOME}/.zsh_history"
HISTSIZE=64000

### Haven't used zsh in so many years, I'll roll with defaults for now.
#setopt appendhistory autocd extendedglob 
#setopt incappendhistory
# dont' beep !
#unsetopt beep
# emacs bindings
#bindkey -e
# use vi ^W behaviour rather then emacs mode default
#bindkey "^W" vi-backward-kill-word
#zstyle :compinstall filename "$HOME/.zshrc"
####

# Real tab completion.
#
# If compinit complains about permissions (see compaudit), -C will skip
# asking again if there is already a ~/.zcompdump file.
#
autoload -Uz compinit && compinit -C

# Compat with bash-completion.
autoload -Uz bashcompinit && bashcompinit

PS1="[ %n@%m ]$ "
export PS1
# display the (24h) time in the right-prompt`
#RPS1="%T"; export RPS1

